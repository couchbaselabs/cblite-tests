// this writes documents to the database and watches the changes feed
// it can also validate channels for sync gateway
// it can read changes from a sync gateway and write to a couchbase lite
// or write directly to the sync gateway
