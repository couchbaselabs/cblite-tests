{
	"log": ["CRUD", "REST+"],
	"databases": {
		"db": {
			"server": "http://localhost:8091",
			"sync": `
function(doc){
	channel(doc.channels);
}`,
			"users": {
				"GUEST": {"disabled": false, "admin_channels": ["*"] }
			}
		}
	}
}
